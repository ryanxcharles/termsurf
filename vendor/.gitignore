# Analysis repos (not committed)
wezterm/
electron/
alacritty/
ghostty/
edtui/

# libghostty dependency sources — reimplementation references (Issue 801).
# Cloned for reading/study only; reimplemented in Rust, never vendored/linked.
# Zig-origin libraries
libxev/
vaxis/
z2d/
zig-objc/
zig-js/
uucode/
zf/
# C-origin libraries
harfbuzz/
freetype/
wuffs/
oniguruma/
simdutf/
highway/
libpng/
zlib/
sentry-native/
imgui/
dear_bindings/
glslang/
spirv-cross/
