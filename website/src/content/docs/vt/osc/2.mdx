---
title: Change Window Title (OSC 2)
navLabel: OSC 2
description: Change the name of the window title.
section: Terminal API
subsection: OSC
order: 7
---

# Change Window Title (OSC 2)

<VTSequence sequence={["OSC", "2", ";", "Pt", "ST"]} />

Change the window title to `t`.

While xterm defaults to interpreting `t` as a ISO-8859-1-encoded string,
which can be changed via Set Title Mode (XTSMTITLE) to either accept a
hexadecimal-escaped or bare UTF-8 string, TermSurf always unconditionally
expects `t` to be directly UTF-8-encoded.
