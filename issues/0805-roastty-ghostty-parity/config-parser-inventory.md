# Config Parser Inventory

Generated by `issues/0805-roastty-ghostty-parity/config_parser_inventory.py` for
Issue 805 Experiment 13.

## Counts

| Category                       | Count |
| ------------------------------ | ----: |
| Canonical parser rows          |   203 |
| Compatibility-only parser arms |     5 |
| Oracle complete rows           |    39 |
| Audit covered rows             |   164 |
| Gap rows                       |     0 |

## Parser Families

| Parser family     | Count |
| ----------------- | ----: |
| boolean           |    40 |
| color             |    16 |
| command palette   |     1 |
| custom inline     |    15 |
| custom parse_cli  |    14 |
| duration          |     4 |
| enum              |    52 |
| float scalar      |     9 |
| font              |    15 |
| integer scalar    |    10 |
| key binding       |     2 |
| packed flags      |     9 |
| path              |     3 |
| string            |     9 |
| unsupported       |     1 |
| window padding    |     2 |
| working directory |     1 |

## Compatibility-Only Parser Arms

These arms are not canonical Ghostty option rows for CFG-217.

- `adw-toolbar-style`
- `background-blur-radius`
- `bold-is-bright`
- `cursor-invert-fg-bg`
- `selection-invert-fg-bg`

## Rows

| ID        | Canonical option                        | Parser path/helper                                        | Parser family     | Status          | Evidence                                                                                                                                        | Missing evidence                                                                                     |
| --------- | --------------------------------------- | --------------------------------------------------------- | ----------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| PARSE-001 | `abnormal-command-exit-runtime`         | `set_value_field, parse_u32_scalar_field`                 | integer scalar    | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1417`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-002 | `adjust-box-thickness`                  | `set_optional_value_field, parse_metric_modifier`         | custom inline     | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1527`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-003 | `adjust-cell-height`                    | `set_optional_value_field, parse_metric_modifier`         | custom inline     | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1457`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-004 | `adjust-cell-width`                     | `set_optional_value_field, parse_metric_modifier`         | custom inline     | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1450`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-005 | `adjust-cursor-height`                  | `set_optional_value_field, parse_metric_modifier`         | custom inline     | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1520`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-006 | `adjust-cursor-thickness`               | `set_optional_value_field, parse_metric_modifier`         | custom inline     | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1513`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-007 | `adjust-font-baseline`                  | `set_optional_value_field, parse_metric_modifier`         | font              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1464`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-008 | `adjust-icon-height`                    | `set_optional_value_field, parse_metric_modifier`         | custom inline     | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1534`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-009 | `adjust-overline-position`              | `set_optional_value_field, parse_metric_modifier`         | custom inline     | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1499`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-010 | `adjust-overline-thickness`             | `set_optional_value_field, parse_metric_modifier`         | custom inline     | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1506`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-011 | `adjust-strikethrough-position`         | `set_optional_value_field, parse_metric_modifier`         | custom inline     | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1485`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-012 | `adjust-strikethrough-thickness`        | `set_optional_value_field, parse_metric_modifier`         | custom inline     | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1492`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-013 | `adjust-underline-position`             | `set_optional_value_field, parse_metric_modifier`         | custom inline     | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1471`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-014 | `adjust-underline-thickness`            | `set_optional_value_field, parse_metric_modifier`         | custom inline     | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1478`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-015 | `alpha-blending`                        | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1446`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-016 | `app-notifications`                     | `set_packed_field, ::parse_cli`                           | packed flags      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1955`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-017 | `async-backend`                         | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1904`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-018 | `auto-update`                           | `set_optional_enum_field, from_keyword`                   | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1908`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-019 | `auto-update-channel`                   | `set_optional_enum_field, from_keyword`                   | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1912`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-020 | `background`                            | `set_value_field, ::parse_cli`                            | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2060`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-021 | `background-blur`                       | `.parse_cli`                                              | custom parse_cli  | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2191`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-022 | `background-image`                      | `set_optional_value_field`                                | path              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1572`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-023 | `background-image-fit`                  | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1579`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-024 | `background-image-opacity`              | `set_f32_field`                                           | float scalar      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2053`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-025 | `background-image-position`             | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1565`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-026 | `background-image-repeat`               | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:2040` | None for direct boolean parser semantics.                                                            |
| PARSE-027 | `background-opacity`                    | `set_f64_field`                                           | float scalar      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2050`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-028 | `background-opacity-cells`              | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:2056` | None for direct boolean parser semantics.                                                            |
| PARSE-029 | `bell-audio-path`                       | `set_optional_value_field`                                | path              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1398`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-030 | `bell-audio-volume`                     | `set_f64_field`                                           | float scalar      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1395`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-031 | `bell-features`                         | `set_packed_field, ::parse_cli`                           | packed flags      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1951`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-032 | `bold-color`                            | `set_optional_value_field, ::parse_cli`                   | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2117`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-033 | `class`                                 | `set_optional_value_field, parse_string_field`            | string            | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1695`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-034 | `click-repeat-interval`                 | `set_value_field`                                         | custom inline     | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1369`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-035 | `clipboard-codepoint-map`               | `.parse_cli, ::parse_cli`                                 | custom parse_cli  | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2186`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-036 | `clipboard-paste-bracketed-safe`        | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1335` | None for direct boolean parser semantics.                                                            |
| PARSE-037 | `clipboard-paste-protection`            | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1331` | None for direct boolean parser semantics.                                                            |
| PARSE-038 | `clipboard-read`                        | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1316`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-039 | `clipboard-trim-trailing-spaces`        | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1327` | None for direct boolean parser semantics.                                                            |
| PARSE-040 | `clipboard-write`                       | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1320`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-041 | `command`                               | `set_optional_value_field, ::parse_cli`                   | custom parse_cli  | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2033`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-042 | `command-palette-entry`                 | `.parse_cli`                                              | command palette   | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1387`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-043 | `config-default-files`                  | `set_bool_field`                                          | boolean           | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1374`                        | Direct parser and effective default-file load-order semantics must be proven together under CFG-221. |
| PARSE-044 | `config-file`                           | `.parse_cli`                                              | path              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1373`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-045 | `confirm-close-surface`                 | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1586`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-046 | `copy-on-select`                        | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1304`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-047 | `cursor-click-to-move`                  | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:2089` | None for direct boolean parser semantics.                                                            |
| PARSE-048 | `cursor-color`                          | `set_optional_value_field, ::parse_cli`                   | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2066`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-049 | `cursor-opacity`                        | `set_f64_field`                                           | float scalar      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2070`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-050 | `cursor-style`                          | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2071`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-051 | `cursor-style-blink`                    | `set_optional_value_field`                                | custom inline     | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2075`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-052 | `cursor-text`                           | `set_optional_value_field, ::parse_cli`                   | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2079`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-053 | `custom-shader`                         | `.parse_cli`                                              | custom parse_cli  | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1943`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-054 | `custom-shader-animation`               | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1944`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-055 | `desktop-notifications`                 | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1893` | None for direct boolean parser semantics.                                                            |
| PARSE-056 | `enquiry-response`                      | `set_value_field, parse_string_field`                     | string            | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1900`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-057 | `env`                                   | `.parse_cli`                                              | custom parse_cli  | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1412`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-058 | `faint-opacity`                         | `set_f64_field`                                           | float scalar      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2126`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-059 | `focus-follows-mouse`                   | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1685` | None for direct boolean parser semantics.                                                            |
| PARSE-060 | `font-codepoint-map`                    | `.parse_cli`                                              | font              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2185`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-061 | `font-family`                           | `.parse_cli`                                              | font              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2147`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-062 | `font-family-bold`                      | `.parse_cli`                                              | font              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2148`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-063 | `font-family-bold-italic`               | `.parse_cli`                                              | font              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2150`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-064 | `font-family-italic`                    | `.parse_cli`                                              | font              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2149`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-065 | `font-feature`                          | `.parse_cli`                                              | font              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2173`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-066 | `font-shaping-break`                    | `set_packed_field, ::parse_cli`                           | packed flags      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1962`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-067 | `font-size`                             | `set_f32_field`                                           | float scalar      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2174`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-068 | `font-style`                            | `set_value_field, ::parse_cli`                            | font              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2151`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-069 | `font-style-bold`                       | `set_value_field, ::parse_cli`                            | font              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2154`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-070 | `font-style-bold-italic`                | `set_value_field, ::parse_cli`                            | font              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2162`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-071 | `font-style-italic`                     | `set_value_field, ::parse_cli`                            | font              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2158`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-072 | `font-synthetic-style`                  | `set_packed_field, ::parse_cli`                           | packed flags      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2166`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-073 | `font-thicken`                          | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1969` | None for direct boolean parser semantics.                                                            |
| PARSE-074 | `font-thicken-strength`                 | `set_value_field, parse_u8_field`                         | integer scalar    | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1970`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-075 | `font-variation`                        | `custom inline parser`                                    | font              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2175`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-076 | `font-variation-bold`                   | `custom inline parser`                                    | font              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2176`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-077 | `font-variation-bold-italic`            | `custom inline parser`                                    | font              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2182`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-078 | `font-variation-italic`                 | `custom inline parser`                                    | font              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2179`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-079 | `foreground`                            | `set_value_field, ::parse_cli`                            | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2063`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-080 | `freetype-load-flags`                   | `set_packed_field, ::parse_cli`                           | packed flags      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1926`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-081 | `fullscreen`                            | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1688`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-082 | `grapheme-width-method`                 | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1919`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-083 | `gtk-custom-css`                        | `.parse_cli`                                              | custom parse_cli  | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1892`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-084 | `gtk-opengl-debug`                      | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1840` | None for direct boolean parser semantics.                                                            |
| PARSE-085 | `gtk-quick-terminal-layer`              | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1261`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-086 | `gtk-quick-terminal-namespace`          | `set_value_field, parse_string_field`                     | string            | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1268`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-087 | `gtk-single-instance`                   | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1843`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-088 | `gtk-tabs-location`                     | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1855`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-089 | `gtk-titlebar`                          | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1854` | None for direct boolean parser semantics.                                                            |
| PARSE-090 | `gtk-titlebar-hide-when-maximized`      | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1866` | None for direct boolean parser semantics.                                                            |
| PARSE-091 | `gtk-titlebar-style`                    | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1884`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-092 | `gtk-toolbar-style`                     | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1870`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-093 | `gtk-wide-tabs`                         | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1891` | None for direct boolean parser semantics.                                                            |
| PARSE-094 | `image-storage-limit`                   | `set_value_field, parse_u32_scalar_field`                 | integer scalar    | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1340`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-095 | `initial-command`                       | `set_optional_value_field, ::parse_cli`                   | custom parse_cli  | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2036`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-096 | `initial-window`                        | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1229` | None for direct boolean parser semantics.                                                            |
| PARSE-097 | `input`                                 | `.parse_cli`                                              | custom parse_cli  | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1413`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-098 | `key-remap`                             | `.parse_cli`                                              | key binding       | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1710`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-099 | `keybind`                               | `.parse_cli`                                              | key binding       | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1709`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-100 | `language`                              | `set_optional_value_field, parse_string_field`            | string            | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2143`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-101 | `link`                                  | `ConfigSetError::NotImplemented`                          | unsupported       | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1431`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-102 | `link-previews`                         | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1593`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-103 | `link-url`                              | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1438` | None for direct boolean parser semantics.                                                            |
| PARSE-104 | `linux-cgroup`                          | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1819`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-105 | `linux-cgroup-hard-fail`                | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1837` | None for direct boolean parser semantics.                                                            |
| PARSE-106 | `linux-cgroup-memory-limit`             | `set_optional_value_field, parse_u64_scalar_field`        | integer scalar    | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1823`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-107 | `linux-cgroup-processes-limit`          | `set_optional_value_field, parse_u64_scalar_field`        | integer scalar    | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1830`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-108 | `macos-applescript`                     | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1783` | None for direct boolean parser semantics.                                                            |
| PARSE-109 | `macos-auto-secure-input`               | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1775` | None for direct boolean parser semantics.                                                            |
| PARSE-110 | `macos-custom-icon`                     | `set_optional_value_field, parse_string_field`            | string            | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1790`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-111 | `macos-dock-drop-behavior`              | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1747`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-112 | `macos-hidden`                          | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1771`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-113 | `macos-icon`                            | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1786`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-114 | `macos-icon-frame`                      | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1794`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-115 | `macos-icon-ghost-color`                | `set_optional_value_field, ::parse_cli`                   | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1801`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-116 | `macos-icon-screen-color`               | `set_optional_value_field, parse_color_list_field`        | custom inline     | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1808`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-117 | `macos-non-native-fullscreen`           | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1726`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-118 | `macos-option-as-alt`                   | `set_optional_enum_field, from_keyword`                   | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1754`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-119 | `macos-secure-input-indication`         | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1779` | None for direct boolean parser semantics.                                                            |
| PARSE-120 | `macos-shortcuts`                       | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1815`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-121 | `macos-titlebar-proxy-icon`             | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1740`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-122 | `macos-titlebar-style`                  | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1733`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-123 | `macos-window-buttons`                  | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1761`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-124 | `macos-window-shadow`                   | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1768` | None for direct boolean parser semantics.                                                            |
| PARSE-125 | `maximize`                              | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1597` | None for direct boolean parser semantics.                                                            |
| PARSE-126 | `middle-click-action`                   | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1362`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-127 | `minimum-contrast`                      | `set_f64_field`                                           | float scalar      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2127`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-128 | `mouse-hide-while-typing`               | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:2092` | None for direct boolean parser semantics.                                                            |
| PARSE-129 | `mouse-reporting`                       | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1351` | None for direct boolean parser semantics.                                                            |
| PARSE-130 | `mouse-scroll-multiplier`               | `.parse_cli`                                              | custom parse_cli  | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1354`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-131 | `mouse-shift-capture`                   | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1344`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-132 | `notify-on-command-finish`              | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1388`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-133 | `notify-on-command-finish-action`       | `set_packed_field, ::parse_cli`                           | packed flags      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1985`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-134 | `notify-on-command-finish-after`        | `set_value_field, ::parse_cli`                            | duration          | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1405`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-135 | `osc-color-report-format`               | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1933`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-136 | `palette`                               | `.parse_cli`                                              | custom parse_cli  | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2130`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-137 | `palette-generate`                      | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:2137` | None for direct boolean parser semantics.                                                            |
| PARSE-138 | `palette-harmonious`                    | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:2140` | None for direct boolean parser semantics.                                                            |
| PARSE-139 | `progress-style`                        | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1896` | None for direct boolean parser semantics.                                                            |
| PARSE-140 | `quick-terminal-animation-duration`     | `set_f64_field`                                           | float scalar      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1282`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-141 | `quick-terminal-autohide`               | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1286` | None for direct boolean parser semantics.                                                            |
| PARSE-142 | `quick-terminal-keyboard-interactivity` | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1297`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-143 | `quick-terminal-position`               | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1247`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-144 | `quick-terminal-screen`                 | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1275`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-145 | `quick-terminal-size`                   | `set_value_field, ::parse_cli`                            | custom parse_cli  | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1254`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-146 | `quick-terminal-space-behavior`         | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1290`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-147 | `quit-after-last-window-closed`         | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1232` | None for direct boolean parser semantics.                                                            |
| PARSE-148 | `quit-after-last-window-closed-delay`   | `set_optional_value_field, ::parse_cli`                   | duration          | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1236`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-149 | `resize-overlay`                        | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1670`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-150 | `resize-overlay-duration`               | `set_value_field, ::parse_cli`                            | duration          | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1681`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-151 | `resize-overlay-position`               | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1674`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-152 | `right-click-action`                    | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1355`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-153 | `scroll-to-bottom`                      | `set_packed_field, ::parse_cli`                           | packed flags      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1974`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-154 | `scrollback-limit`                      | `set_value_field, parse_usize_scalar_field`               | integer scalar    | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1424`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-155 | `scrollbar`                             | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1428`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-156 | `search-background`                     | `set_value_field, ::parse_cli`                            | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2015`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-157 | `search-foreground`                     | `set_value_field, ::parse_cli`                            | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2011`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-158 | `search-selected-background`            | `set_value_field, ::parse_cli`                            | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2026`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-159 | `search-selected-foreground`            | `set_value_field, ::parse_cli`                            | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2019`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-160 | `selection-background`                  | `set_optional_value_field, ::parse_cli`                   | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2103`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-161 | `selection-clear-on-copy`               | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1312` | None for direct boolean parser semantics.                                                            |
| PARSE-162 | `selection-clear-on-typing`             | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1308` | None for direct boolean parser semantics.                                                            |
| PARSE-163 | `selection-foreground`                  | `set_optional_value_field, ::parse_cli`                   | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2096`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-164 | `selection-word-chars`                  | `.parse_cli`                                              | custom parse_cli  | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2116`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-165 | `shell-integration`                     | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1380`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-166 | `shell-integration-features`            | `set_packed_field, ::parse_cli`                           | packed flags      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1978`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-167 | `split-divider-color`                   | `set_optional_value_field, ::parse_cli`                   | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2000`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-168 | `split-inherit-working-directory`       | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1557` | None for direct boolean parser semantics.                                                            |
| PARSE-169 | `split-preserve-zoom`                   | `set_packed_field, ::parse_cli`                           | packed flags      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2004`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-170 | `tab-inherit-working-directory`         | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1553` | None for direct boolean parser semantics.                                                            |
| PARSE-171 | `term`                                  | `set_value_field, parse_string_field`                     | string            | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1899`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-172 | `theme`                                 | `set_optional_value_field, ::parse_cli`                   | custom parse_cli  | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:2198`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-173 | `title`                                 | `set_optional_value_field, parse_string_field`            | string            | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1692`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-174 | `title-report`                          | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1339` | None for direct boolean parser semantics.                                                            |
| PARSE-175 | `undo-timeout`                          | `set_value_field, ::parse_cli`                            | duration          | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1243`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-176 | `unfocused-split-fill`                  | `set_optional_value_field, ::parse_cli`                   | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1996`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-177 | `unfocused-split-opacity`               | `set_f64_field`                                           | float scalar      | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1992`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-178 | `vt-kam-allowed`                        | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1940` | None for direct boolean parser semantics.                                                            |
| PARSE-179 | `wait-after-command`                    | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1414` | None for direct boolean parser semantics.                                                            |
| PARSE-180 | `window-colorspace`                     | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1439`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-181 | `window-decoration`                     | `::parse_cli`                                             | custom parse_cli  | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1602`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-182 | `window-height`                         | `set_value_field, parse_u32_scalar_field`                 | integer scalar    | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1616`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-183 | `window-inherit-font-size`              | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1561` | None for direct boolean parser semantics.                                                            |
| PARSE-184 | `window-inherit-working-directory`      | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1549` | None for direct boolean parser semantics.                                                            |
| PARSE-185 | `window-new-tab-position`               | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1642`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-186 | `window-padding-balance`                | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1719`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-187 | `window-padding-color`                  | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1541`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-188 | `window-padding-x`                      | `set_value_field, ::parse_cli`                            | window padding    | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1711`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-189 | `window-padding-y`                      | `set_value_field, ::parse_cli`                            | window padding    | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1715`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-190 | `window-position-x`                     | `set_optional_value_field, parse_i16_field`               | integer scalar    | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1624`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-191 | `window-position-y`                     | `set_optional_value_field, parse_i16_field`               | integer scalar    | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1628`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-192 | `window-save-state`                     | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1632`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-193 | `window-show-tab-bar`                   | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1649`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-194 | `window-step-resize`                    | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1639` | None for direct boolean parser semantics.                                                            |
| PARSE-195 | `window-subtitle`                       | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1598`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-196 | `window-theme`                          | `set_enum_field, from_keyword`                            | enum              | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1612`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-197 | `window-title-font-family`              | `set_optional_value_field, parse_string_field`            | string            | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1605`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-198 | `window-titlebar-background`            | `set_optional_value_field, ::parse_cli`                   | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1656`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-199 | `window-titlebar-foreground`            | `set_optional_value_field, ::parse_cli`                   | color             | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1663`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-200 | `window-vsync`                          | `set_bool_field`                                          | boolean           | Oracle complete | Shared boolean parser oracle covers upstream true/false spellings, bare true, empty reset, and invalid values; `roastty/src/config/mod.rs:1548` | None for direct boolean parser semantics.                                                            |
| PARSE-201 | `window-width`                          | `set_value_field, parse_u32_scalar_field`                 | integer scalar    | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1620`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-202 | `working-directory`                     | `set_optional_value_field, parse_working_directory_field` | working directory | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1702`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
| PARSE-203 | `x11-instance-name`                     | `set_optional_value_field, parse_string_field`            | string            | Audit covered   | Parser dispatch path identified; option still needs upstream-derived full-value oracle; `roastty/src/config/mod.rs:1698`                        | Full accepted variants/classes plus rejection/reset semantics are not yet proven.                    |
