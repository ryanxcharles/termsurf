# Source Audit

This matrix tracks source-level parity audits between pinned Ghostty and
Roastty/libroastty.

## Row Schema

| Column                     | Meaning                                                                                  |
| -------------------------- | ---------------------------------------------------------------------------------------- |
| ID                         | Stable row ID, prefixed `SRC-`.                                                          |
| Subsystem / source area    | Ghostty subsystem, ABI surface, terminal behavior, renderer path, app bridge, or helper. |
| Upstream behavior / source | Ghostty source path and expected behavior.                                               |
| Roastty behavior / path    | Roastty source path and implemented behavior.                                            |
| Status                     | `Pass`, `Gap`, `Intentional divergence`, or `Not applicable`.                            |
| Verification method        | Source audit method, test, build, or oracle.                                             |
| Evidence artifact          | Log, diff, test output, or matrix row proving the status.                                |
| Guard tier                 | Tier 0-4 from the Issue 805 regression guard policy.                                     |
| Guard command / checklist  | Exact command or manual checklist that catches regressions.                              |
| Run cadence                | When the guard should run.                                                               |
| Guard sufficiency          | Why this guard is strong enough for the row.                                             |
| Owner experiment           | Experiment that created or last updated the row.                                         |
| Notes                      | Short context, if needed.                                                                |

## Rows

| ID      | Subsystem / source area               | Upstream behavior / source                                                                                                           | Roastty behavior / path                                                                                                                                                                                                                                                                                                                                                 | Status | Verification method                                                                                                                                                                                                            | Evidence artifact                                                                                                                                                                  | Guard tier | Guard command / checklist                                                                                                                                            | Run cadence                                                                                     | Guard sufficiency                                                                                                                                         | Owner experiment | Notes                                                                                                                                                         |
| ------- | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SRC-001 | Pinned Ghostty reference source       | `vendor/ghostty` at `2c62d182cec246764ff725096a70b9ef44996f7f` builds from clean source.                                             | Roastty parity target is compared against that clean upstream source.                                                                                                                                                                                                                                                                                                   | Pass   | Build pinned Ghostty with plain Homebrew `zig` and Ghostty's macOS build script.                                                                                                                                               | `logs/issue805-clean-ghostty-zig-build.log`; `logs/issue805-clean-ghostty-app-build.log`; `logs/issue805-exp1-rerun-clean-baseline.log`.                                           | Tier 2     | `cd vendor/ghostty && zig build -Demit-macos-app=false && nu macos/build.nu --configuration Debug`                                                                   | Before source audit milestones and after toolchain changes.                                     | A clean upstream build proves the reference app can be used without local source patches.                                                                 | Experiment 1     | This does not prove Roastty source parity; it pins the source audit reference.                                                                                |
| SRC-002 | App-facing embedded ABI symbols       | Swift app sources under `vendor/ghostty/macos/Sources` use Ghostty embedded ABI identifiers from `vendor/ghostty/include/ghostty.h`. | Swift app sources under `roastty/macos/Sources` use mapped Roastty identifiers declared in `roastty/include/roastty.h`.                                                                                                                                                                                                                                                 | Pass   | Extract upstream and Roastty Swift identifiers with `rg --no-filename -o`, map `ghostty`/`GHOSTTY` prefixes to `roastty`/`ROASTTY`, and compare against Roastty Swift/header identifiers.                                      | `issues/0805-roastty-ghostty-parity/abi-app-symbols.md`; `logs/issue805-exp4-abi-extraction.log`.                                                                                  | Tier 0     | Re-run the Experiment 4 Swift symbol and header identifier extraction commands in `abi-app-symbols.md`.                                                              | Before app bridge, C header, or copied Swift app changes.                                       | Static identifier comparison catches renamed app-facing ABI drift without launching the app.                                                              | Experiment 4     | `ROASTTY_UI_KEY_TRACE_PATH` is Roastty-only automation support and not an upstream ABI requirement.                                                           |
| SRC-003 | App-facing embedded ABI exports       | App-facing Ghostty header functions required by `vendor/ghostty/macos/Sources` are exported by Ghostty's embedded runtime.           | Mapped app-facing Roastty header functions are exported as `#[no_mangle] pub extern "C" fn` definitions in `roastty/src/lib.rs`.                                                                                                                                                                                                                                        | Pass   | Compare `roastty/include/roastty.h` function declarations against `#[no_mangle] pub extern "C" fn roastty_*` definitions and classify regex false positives.                                                                   | `issues/0805-roastty-ghostty-parity/abi-app-symbols.md`; `logs/issue805-exp4-abi-extraction.log`.                                                                                  | Tier 0     | Re-run the Experiment 4 header/export extraction commands in `abi-app-symbols.md`.                                                                                   | Before C header or embedded runtime export changes.                                             | Static export-name comparison catches missing app-facing exported functions cheaply.                                                                      | Experiment 4     | Raw missing `roastty_string_s` is a callback typedef false positive, not a function declaration.                                                              |
| SRC-004 | Full Ghostty header non-app functions | `vendor/ghostty/include/ghostty.h` declares additional public functions outside the `macos/Sources` app-facing slice.                | `roastty/include/roastty.h` declares and `roastty/src/lib.rs` exports the mapped non-app functions. `roastty_app_open_config` dispatches the app runtime action; `roastty_inspector_metal_shutdown` is a valid-handle no-op for Roastty's unsupported Metal inspector backend; `roastty_translate` and `roastty_benchmark_cli` are recorded as intentional divergences. | Pass   | Full-header mapped function comparison reports no missing Roastty declarations; header/export comparison reports only the known `roastty_string_s` callback typedef false positive; C ABI harness covers the new declarations. | `logs/issue805-exp5-static-abi.log`; `logs/issue805-exp5-cargo-test-roastty.log`; `logs/issue805-exp5-roastty-app-build.log`; `issues/0805-roastty-ghostty-parity/divergences.md`. | Tier 0     | Re-run the Experiment 5 full-header and export comparison commands from `05-resolve-non-app-embedded-abi-functions.md`; `cargo test -p roastty -- --test-threads=1`. | Before final source audit certification and before C header or embedded runtime export changes. | Static comparison catches declaration/export drift, and the C ABI harness proves the declared functions link and exercise the important runtime behavior. | Experiment 5     | `DIV-001` records `roastty_translate` identity behavior; `DIV-002` records `roastty_benchmark_cli` returning false because Roastty has no benchmark CLI port. |
